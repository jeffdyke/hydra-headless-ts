# Generated by ci/generate_mariadb_mcp_compose.py -- do not hand-edit.
# Regenerate after any pillar/prod/servers/init.sls mcp_auth_db/
# mcp_default_db/mcp_port change and commit the diff; CI fails the build
# if this drifts from what current pillar would produce.

services:

  mariadb-mcp:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp
    env_file: /etc/hydra-headless-ts/mariadb-mcp.env
    ports:
      - "9001:9001"

  mariadb-mcp-proddb01:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp-proddb01
    env_file: /etc/hydra-headless-ts/mariadb-mcp-proddb01.env
    ports:
      - "9010:9001"

  mariadb-mcp-proddb02:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp-proddb02
    env_file: /etc/hydra-headless-ts/mariadb-mcp-proddb02.env
    ports:
      - "9011:9001"

  mariadb-mcp-proddb03:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp-proddb03
    env_file: /etc/hydra-headless-ts/mariadb-mcp-proddb03.env
    ports:
      - "9012:9001"
