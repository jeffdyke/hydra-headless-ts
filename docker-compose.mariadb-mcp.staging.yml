# Generated by ci/generate_mariadb_mcp_compose.py -- do not hand-edit.
# Regenerate after any pillar/staging/servers/init.sls mcp_auth_db/
# mcp_default_db/mcp_port change and commit the diff; CI fails the build
# if this drifts from what current pillar would produce.

services:

  mariadb-mcp-stagingdb01:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp-stagingdb01
    env_file: /etc/hydra-headless-ts/mariadb-mcp-stagingdb01.env
    ports:
      - "9012:9001"

  mariadb-mcp-stagingdb02:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp-stagingdb02
    env_file: /etc/hydra-headless-ts/mariadb-mcp-stagingdb02.env
    ports:
      - "9011:9001"

  mariadb-mcp-stagingdb05:
    extends:
      file: docker-compose.yml
      service: mariadb-mcp-base
    container_name: mariadb-mcp-stagingdb05
    env_file: /etc/hydra-headless-ts/mariadb-mcp-stagingdb05.env
    ports:
      - "9010:9001"
